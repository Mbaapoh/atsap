// Stage 1: connect to AMI, read the banner, log in by hand.
// See README.md in this folder for what to do.
package main

func main() {
	// TODO:
	// 1. Read AMI_ADDR, AMI_USERNAME, AMI_PASSWORD from the environment.
	// 2. net.Dial("tcp", addr).
	// 3. Read and print the banner line.
	// 4. Write a Login action (Action/Username/Secret + blank line).
	// 5. Read and print the response until the blank line.
}
