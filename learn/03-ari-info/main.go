// Stage 3: authenticated GET against ARI, decoded into a struct.
// See README.md in this folder.
package main

func main() {
	// TODO:
	// 1. Read ARI_URL, ARI_USERNAME, ARI_PASSWORD from the environment.
	// 2. Build a GET request to ARI_URL + "/asterisk/info".
	// 3. req.SetBasicAuth(username, password).
	// 4. Send it, check the status code.
	// 5. Decode the JSON body into a struct and print system.version.
}
