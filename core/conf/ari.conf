; Asterisk REST Interface. Credentials here are for local development only
; (they must match ARI_USERNAME/ARI_PASSWORD in your .env). For production,
; ship a separate conf/ overlay with strong, unique credentials — see
; README.md.
[general]
enabled = yes
pretty = yes

[voipapp]
type = user
read_only = no
password = devpassword123
