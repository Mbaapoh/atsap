; Logs Channel Event Log (CEL) records to Postgres. Requires cel.conf's
; [general] enable=yes to actually generate events. Credentials here are
; for local development only; see cdr_pgsql.conf for the same note.
[global]
hostname=postgres
port=5432
dbname=asterisk
user=asterisk
password=devpassword123
table=cel
